# Streamlit Cutover Archive

This directory keeps historical migration planning and validation documents from the
`Streamlit -> FastAPI + Next.js` transition.

These files are retained for traceability, but they no longer describe the active
runtime topology of the repository.

Current active references:

- `README.md`
- `docs/architecture.md`
- `web/README.md`
